<?xml version="1.0" encoding="ISO-8859-1"?>

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
   xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>@GROUP_ID@</groupId>
  <artifactId>@ARTIFACT_ID@</artifactId>
  <packaging>jar</packaging>
  <name>JSimpleDB</name>
  <version>@VERSION@</version>
  <description>JSimpleDB makes powerful persistence easy for Java programmers.

JSimpleDB's goal is to make scalable Java persistence as simple as possible, exposing complexity to the developer only when there is inherent underlying complexity in the problem space, and even then doing so in the most straightforward manner, all the while remaining strictly type safe.

JSimpleDB does this by relegating the database to the simplest role possible - storing data as key/value pairs - and reimplementing the usual database supporting features, such as indexes and notifications, in a simpler, type-safe, Java-centric way. JSimpleDB also adds important new features that traditional databases don't provide.</description>
  <url>http://code.google.com/p/jsimpledb/</url>
  <licenses>
    <license>
      <name>The Apache Software License, Version 2.0</name>
      <url>http://www.apache.org/licenses/LICENSE-2.0.txt</url>
      <distribution>repo</distribution>
    </license>
  </licenses>
  <issueManagement>
    <system>Google Code</system>
    <url>http://code.google.com/p/jsimpledb/issues</url>
  </issueManagement>
  <scm>
    <url>https://github.com/archiecobbs/jsimpledb/</url>
    <connection>scm:git:git://github.com/archiecobbs/jsimpledb.git</connection>
    <developerConnection>scm:git:git://github.com/archiecobbs/jsimpledb.git</developerConnection>
  </scm>
  <developers>
    <developer>
      <id>archie</id>
      <name>Archie Cobbs</name>
      <email>archie@dellroad.org</email>
    </developer>
  </developers>
  <dependencies>
    <dependency>
      <groupId>javax.validation</groupId>
      <artifactId>validation-api</artifactId>
      <version>1.1.0.Final</version>
    </dependency>
    <dependency>
      <groupId>com.google.guava</groupId>
      <artifactId>guava</artifactId>
      <version>@GUAVA_VERSION@</version>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-api</artifactId>
      <version>@SLF4J_VERSION@</version>
    </dependency>
    <dependency>
      <groupId>org.dellroad</groupId>
      <artifactId>dellroad-stuff</artifactId>
      <version>@DRSTUFF_VERSION@</version>
    </dependency>
  </dependencies>
  <parent>
    <groupId>org.sonatype.oss</groupId>
    <artifactId>oss-parent</artifactId>
    <version>7</version>
  </parent>
</project>
